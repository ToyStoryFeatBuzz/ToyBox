namespace ToyBox {
    public  static class Enums {
        public enum EPlayerState {
            Dead,
            Alive,
            Finished
        }
        
        public enum EWallJumpDirection
        {
            Left,
            Right,
            None
        }
    }
}